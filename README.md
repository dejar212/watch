# 📐 Compact Math Framework

Фреймворк для компактного отображения математических задач в HTML формате с темной темой и оптимальным использованием пространства.

## 🎯 Особенности

- **Компактная сеточная верстка** (CSS Grid) - задачи размещаются оптимально
- **Темная тема** - черный фон, белый текст для удобства чтения
- **MathJax** - красивый рендеринг LaTeX формул
- **Адаптивность** - подходит для печати и экрана
- **Визуализации** - автоматическая генерация графиков через matplotlib (опционально)
- **Standalone HTML** - один файл со всеми стилями

## 📁 Структура проекта

```
watch/
├── framework/
│   ├── generator.py          # Основной генератор
│   ├── visualizations.py     # Модуль графических иллюстраций
│   ├── template.html         # HTML шаблон
│   ├── styles.css           # Компактные стили
│   └── config.json          # Конфигурация
├── generate_matem.py        # Скрипт с визуализациями (требует matplotlib)
├── generate_matem_simple.py # Упрощенный скрипт без визуализаций
├── matem_22_11_compact.html # Сгенерированный результат
└── README.md                # Эта документация
```

## 🚀 Быстрый старт

### 1. Генерация без визуализаций (рекомендуется для начала)

```bash
cd watch
python3 generate_matem_simple.py
```

Откройте `matem_22_11_compact.html` в браузере.

### 2. Генерация с визуализациями

Сначала установите matplotlib:

```bash
pip install matplotlib
```

Затем запустите:

```bash
python3 generate_matem.py
```

## 📚 Использование фреймворка

### Базовый пример

```python
from generator import CompactFramework, create_task

# Создаем фреймворк
framework = CompactFramework(
    title="Моя работа",
    author="Ваше имя",
    date="Дата"
)

# Добавляем задачи
framework.add_task(create_task(
    number=1,
    category='short',  # 'short', 'medium', 'long'
    solution=r'''
        Решим уравнение \( x^2 = 4 \)
        <br>
        \[ x = \pm 2 \]
        <div class="answer">Ответ: x = ±2</div>
    '''
))

# Генерируем HTML
framework.generate_standalone('output.html')
```

### Категории задач

Фреймворк автоматически размещает задачи в зависимости от категории:

- **`short`** - Короткие задачи (2 в ряд)
  - Простые вычисления
  - Определители 2×2
  - Короткие доказательства
  
- **`medium`** - Средние задачи (1 на колонку)
  - Векторные операции
  - Определители 3×3
  - Задачи с несколькими шагами
  
- **`long`** - Длинные задачи (на всю ширину)
  - Системы уравнений
  - Сложные вычисления
  - Задачи с большим количеством формул

### Добавление визуализаций

```python
from visualizations import plot_vectors_2d_3d

# Создаем визуализацию
viz = plot_vectors_2d_3d(
    vectors=[(1, 2, 3), (4, 5, 6)],
    labels=['a', 'b'],
    title="Векторы"
)

# Добавляем задачу с визуализацией
framework.add_task(create_task(
    number=1,
    category='medium',
    solution='Текст решения...',
    visualization_svg=viz
))
```

### Доступные функции визуализации

```python
from visualizations import (
    plot_parallelogram,      # Параллелограмм на векторах
    plot_pyramid,           # Пирамида/тетраэдр
    plot_line_and_point,    # Прямая и точка с проекцией
    plot_plane,            # Плоскость
    plot_two_lines,        # Две прямые в пространстве
    plot_matrix_heatmap    # Тепловая карта матрицы
)
```

## 🎨 LaTeX формулы

### Inline формулы

```html
Решим уравнение \( ax^2 + bx + c = 0 \)
```

### Block формулы

```html
\[ x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a} \]
```

### Матрицы

```html
\[ A = \begin{pmatrix}
1 & 2 & 3 \\
4 & 5 & 6 \\
7 & 8 & 9
\end{pmatrix} \]
```

### Системы уравнений

```html
\[ \begin{cases}
x + y = 5 \\
x - y = 1
\end{cases} \]
```

## 🎛️ Настройка

Отредактируйте `framework/config.json`:

```json
{
  "page_settings": {
    "background": "#000000",
    "text_color": "#ffffff",
    "square_size": "15cm"
  },
  "task_categories": {
    "short": {"width": "0.5", "height": "1"},
    "medium": {"width": "1", "height": "1"},
    "long": {"width": "2", "height": "1"}
  }
}
```

## 📝 Стилизация решений

### Выделение ответа

```html
<div class="answer">Ответ: x = 42</div>
```

### Компактный режим

```html
<div class="compact">
Текст с уменьшенными отступами
</div>
```

### Двухколоночная компоновка

```html
<div class="two-column">
    <div class="left-col">Левая часть</div>
    <div class="right-col">Правая часть</div>
</div>
```

### Шаги решения

```html
<div class="solution-steps">
    <div class="solution-step">
        <span class="step-number">1.</span>
        <span class="step-content">Первый шаг</span>
    </div>
    <div class="solution-step">
        <span class="step-number">2.</span>
        <span class="step-content">Второй шаг</span>
    </div>
</div>
```

## 🖨️ Печать

Документ оптимизирован для печати на A4:
- Автоматическое разбиение на страницы
- Сохранение структуры задач
- Адаптация цветов для печати

Просто откройте HTML в браузере и выберите `Печать` (Ctrl+P / Cmd+P).

## 💡 Советы по оптимизации пространства

1. **Группируйте короткие задачи** - они автоматически размещаются по 2 в ряд
2. **Используйте компактные обозначения** - т.к., т.е., ур-е и т.д.
3. **Минимизируйте текст** - только суть решения
4. **Используйте two-column** для задач с вычислениями и графиками
5. **Избегайте длинных текстовых описаний** - максимум формул

## 🔧 Продвинутое использование

### Создание пользовательских стилей

```python
framework.add_custom_styles('''
.my-custom-class {
    background-color: #1a1a1a;
    border: 1px solid #4ecdc4;
}
''')
```

### Автоматическая классификация задач

```python
def classify_task(solution_text):
    """Автоматически определяет категорию по длине текста"""
    length = len(solution_text)
    if length < 200:
        return 'short'
    elif length < 500:
        return 'medium'
    else:
        return 'long'
```

## 🐛 Решение проблем

### Формулы не отображаются

Проверьте подключение к интернету - MathJax загружается с CDN.

### Визуализации не работают

Установите matplotlib:
```bash
pip install matplotlib
```

### HTML выглядит неправильно

Используйте современный браузер (Chrome, Firefox, Safari).

## 📦 Копирование в другой проект

Скопируйте всю папку `watch/` в новый проект. Фреймворк полностью независим от остального кода.

```bash
cp -r watch/ /путь/к/новому/проекту/
```

## 🎓 Примеры

Смотрите `generate_matem_simple.py` для полного примера создания контрольной работы с 13 задачами.

## 📄 Лицензия

Свободное использование для учебных целей.

---

**Создано для оптимального отображения математических работ** 🚀

